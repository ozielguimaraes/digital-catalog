<?xml version="1.0" encoding="utf-8"?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:components="clr-namespace:MeuCatalogo.Components"
             x:Class="MeuCatalogo.Components.ProductGridCard"
             x:Name="Root">
    <Border Style="{StaticResource CardElevated}" Padding="0">
        <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding TapCommand, Source={x:Reference Root}}"
                                  CommandParameter="{Binding TapCommandParameter, Source={x:Reference Root}}" />
        </Border.GestureRecognizers>
        <Grid RowDefinitions="Auto,Auto" RowSpacing="0">
            <Border Grid.Row="0"
                    StrokeThickness="0"
                    HeightRequest="140"
                    BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceMutedColorLight}, Dark={StaticResource SurfaceMutedColorDark}}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="18,18,0,0" />
                </Border.StrokeShape>
                <Image Source="{Binding ThumbnailSource, Source={x:Reference Root}}"
                       Aspect="AspectFill" />
            </Border>

            <Grid Grid.Row="1"
                  Padding="12,10,12,12"
                  RowDefinitions="Auto,Auto,Auto"
                  RowSpacing="6">
                <Label Grid.Row="0"
                       Text="{Binding Nome, Source={x:Reference Root}}"
                       FontFamily="LatoBold"
                       FontSize="14"
                       LineBreakMode="TailTruncation"
                       MaxLines="2"
                       TextColor="{AppThemeBinding Light={StaticResource TextColorLight}, Dark={StaticResource TextColorDark}}" />

                <components:PriceTag Grid.Row="1"
                                     Preco="{Binding Preco, Source={x:Reference Root}}"
                                     PrecoComDesconto="{Binding PrecoComDesconto, Source={x:Reference Root}}"
                                     IsCompact="True" />

                <Label Grid.Row="2"
                       Text="{Binding SubtitleText, Source={x:Reference Root}}"
                       Style="{StaticResource BodyMuted}"
                       FontSize="11"
                       IsVisible="{Binding HasSubtitle, Source={x:Reference Root}}" />
            </Grid>
        </Grid>
    </Border>
</ContentView>
